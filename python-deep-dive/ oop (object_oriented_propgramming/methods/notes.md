## Methods

# What are methods
  >> Methods are functions that define the behavior of objects created from class
  >> All methods must have self as first parameter
  >> There can accept parameters just like any other methods
  >> Methods can access and modify object properties using self
  >> The __str__() is a special method that defines what is displayed when the object is printed
  >> You can use del keyword to delete methods and objects
  