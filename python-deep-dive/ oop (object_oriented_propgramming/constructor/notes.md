## __init__ method

# What is __init__() method
  >> All classes have built-in init method
  >> Always executes when the class is being executed
  >> Used to perform necessary calculations and assign values to object properties.
  >> You can also define how objects values should handle and type of calculations. 
  >> This method is called automatically when the class is being used.
  