## Read Files

>> Use the open() function to open a file which returns a file object 
>> The file object has read() method for reading the contents of the file
>> you can specify the path if the file is located in a different location
>> You can use the with statement when opening the file - you don't have to worry about closing the file
